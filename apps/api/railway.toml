[build]
builder = "NIXPACKS"
buildCommand = "cd ../.. && npm ci && npm run db:generate && npm run build"

[deploy]
startCommand = "cd apps/api && sh start.sh"
healthcheckPath = "/health"
healthcheckTimeout = 30
